program PrjLagrange;

uses
  Forms,
  UnitLagrange in 'UnitLagrange.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Lagrange';
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
